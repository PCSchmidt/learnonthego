[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.setup]
aptPkgs = ["gcc", "g++", "libpq-dev", "curl"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
